%p{:style => "color: green"}= notice

%div{style: "margin: 0 0 20px 0;"}
  = link_to "Back to jet fuels", jet_fuels_path

= render @jet_fuel

%br

.btn-toolbar{role: "toolbar", "aria-label": "Toolbar with button groups"}
  = link_to "Edit", edit_jet_fuel_path(@jet_fuel), class: "btn btn-primary"
  &nbsp;
  = button_to "Destroy this jet fuel", @jet_fuel, method: :delete, class: "btn btn-primary"